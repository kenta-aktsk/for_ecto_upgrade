<h2>Edit tag</h2>

<%= render "form.html", changeset: @changeset,
                        tag: @tag,
                        action: admin_tag_path(@conn, :update, @conn.assigns.locale, @tag) %>

<%= link "Back", to: admin_tag_path(@conn, :index, @conn.assigns.locale) %>
